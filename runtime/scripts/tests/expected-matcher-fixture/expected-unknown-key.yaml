# Top-level key not in the allowed set {must_contain, must_not_contain}.
# Should fail with: ERROR expected_yaml_unknown_top_level_key key=bogus_section file=<path>  (exit 2)
must_contain:
  agents: [foo]
bogus_section: 1
